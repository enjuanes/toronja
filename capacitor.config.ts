import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'web.senjuanes.toronja',
  appName: 'toronja',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
